# Stream Analytics Dashboard

The dashboard is a Next.js App Router application that preserves the existing
React components and client-side dashboard navigation.

## Development

```bash
npm install
npm run dev
```

Open http://localhost:3000.

## Production

```bash
npm run build
npm start
```

Set `NEXT_PUBLIC_API_URL` to override the default API URL
(`http://localhost:8000`).
